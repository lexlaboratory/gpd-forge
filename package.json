{
  "name": "gpd-forge-e2e",
  "private": true,
  "description": "Repo-root tooling for GPD Forge end-to-end (Playwright) tests. The app itself lives in ui/.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
